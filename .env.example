# OpenRouter API Key (可选)
# 如果设置了此环境变量，将作为默认 API Key 使用
# 用户仍可在前端界面手动输入并覆盖此配置
OPENROUTER_API_KEY=sk-or-**********

# AI 模型 (可选)
# 默认使用 xiaomi/mimo-v2-flash:free
# 可改为其他 OpenRouter 支持的模型，如 anthropic/claude-3-5-sonnet
OPENROUTER_MODEL=xiaomi/mimo-v2-flash:free
